# AGENTS.md

このファイルは、このリポジトリで開発・調査・執筆を行うAIエージェントと人間の共同作業者に対する最上位の作業規約です。より深いディレクトリに別の `AGENTS.md` がある場合は、対象ファイルに近い規約を優先します。

## 1. リポジトリの目的

既存の `sec*`、`scenario*.py`、`doc/*.ipynb` は、ネットワークの正常動作を章ごとの差分で学ぶ前作教材です。次期教材は `security-course/` 配下で開発し、次の学習サイクルを一貫して提供します。

> 正常系を観測する → 攻撃をシミュレーションする → 検知・防御を実装する → 指標で比較する → 現実との差を説明する

前作の教育的価値を維持しつつ、次期教材では共通コア、仕様、テスト、Colab、書籍原稿を分離し、AIエージェントが小さな垂直スライス単位で安全に更新できるようにします。

## 2. 作業開始前に読む順序

`security-course/` に変更を加える前に、最低限次を順に確認してください。

1. `security-course/README.md`
2. `security-course/catalog.json`
3. `security-course/docs/PROJECT.md`
4. `security-course/docs/ENGINEERING.md`
5. `security-course/docs/WRITING.md`（原稿・Notebook・説明文を変更する場合）
6. 対象章の `security-course/specs/SEC-XX-*.md`
7. `security-course/docs/TRACEABILITY.md`

仕様と実装が矛盾する場合、黙ってどちらかへ合わせず、同じPR内で仕様・実装・テスト・追跡表を整合させます。

## 3. 絶対的な安全境界

本プロジェクトは**閉じたシミュレータによる教育**だけを対象とします。

### 許可される実装

- Pythonオブジェクトとしてのパケット、フレーム、キャッシュ、キュー、ログ
- インメモリまたはテスト用一時ファイル内のイベント処理
- 架空のIPアドレス、MACアドレス、認証情報、証明書
- 攻撃前後の状態比較、検知、緩和策、誤検知・副作用の評価
- RFCや公式文書を根拠にしたプロトコル説明

### 禁止される実装

- `socket`、raw socket、Scapy等による実パケット送受信・注入・盗聴
- 実ネットワークの探索、ポートスキャン、サービス列挙
- OSのARP/DNS/ルーティングテーブル、ファイアウォール設定の変更
- 認証情報、Cookie、鍵、トークン、個人データの取得・保存
- 外部ホストに対する負荷試験、DoS、リレー、プロキシ動作
- 回避、永続化、痕跡消去、実環境向け自動化
- 「教育用」という説明だけを付けた実運用可能な攻撃コード

新しい依存パッケージが実ネットワーク機能を持つ場合は、使わなくても導入理由と安全レビューが必要です。既定では標準ライブラリを優先します。

## 4. 変更の基本単位

一つのPRは、原則として一つの章IDまたは一つの横断的基盤変更に限定します。

章の垂直スライスは、可能な限り次を同時に更新します。

1. `catalog.json` の状態と成果物パス
2. `specs/` の要件・受入条件
3. `src/` のシミュレータ実装
4. `tests/` の正常系・攻撃系・防御系・安全境界テスト
5. `notebooks/` の実験手順
6. `manuscript/` の原稿
7. `docs/TRACEABILITY.md` の対応表
8. `references/sources.json` の根拠資料

実装だけ、原稿だけを先行させる場合も、未整備の成果物と次の作業を仕様に明記してください。

## 5. 開発手順

1. `catalog.json` から章IDを選びます。
2. 対象章の仕様を作成または更新し、学習目標、脅威モデル、機能要件、非機能要件、受入条件をID付きで定義します。
3. 正常系を先に実装し、期待するイベント列をテストします。
4. 攻撃はシミュレータ内部の状態遷移として追加します。
5. 検知と防御を別の構成要素として実装し、攻撃成功率だけでなく正常通信への影響も測ります。
6. 同一条件で防御なし・防御ありを比較できるAPIを用意します。
7. Notebookと原稿は、テスト済みの公開APIだけを使用します。
8. 検証コマンドを実行し、追跡表を更新します。

## 6. コード規約

- Python 3.11以上を対象とし、公開APIには型注釈を付けます。
- シナリオは同じ入力から同じ結果を返す決定的な実装を既定とします。
- 乱数が必要な場合は、シードを引数で受け取り、結果へ記録します。
- 学習者が追える小さなクラスと明示的なイベントを優先します。
- 重要な状態変更は `SecurityEvent` 相当のイベントとして記録します。
- Notebookや原稿が内部属性へ依存しないよう、安定した結果オブジェクトを返します。
- 現実のプロトコルを完全再現しているように見せないでください。省略点をdocstring、仕様、原稿のいずれかに明記します。
- 既存の `sec*` を横断的にリファクタリングしません。前作との接続は、専用アダプタを設計してから行います。
- 教材コードの短さを理由にテストを省略しません。

## 7. テストの最低要件

章プロトタイプには次の観点を含めます。

- 正常通信が成立する
- 防御なしで想定した攻撃条件が成立する
- 防御ありで攻撃条件が抑止または緩和される
- 正常通信の成功率、遅延、可用性などの副作用を確認できる
- イベント列が再現可能である
- 実ネットワーク機能を導入していない
- `catalog.json` と成果物パスが一致する
- Notebookに実行結果や秘密情報が保存されていない

## 8. 執筆規約

- 本文は原則として日本語で書き、対象読者は「Pythonの基礎を学んだネットワーク初学者」とします。
- 用語を先に定義し、その後にコードとイベント列を示します。
- 各章は「正常系・成立条件・攻撃・観測・検知・防御・評価・現実との差・演習」の順を基本とします。
- 攻撃手順を刺激的に見せることより、成立条件と防御の設計判断を重視します。
- 事実と教材上の仮定を明確に区別します。「このシミュレータでは」と「実装一般では」を混同しません。
- 外部事実はRFC、標準仕様、公式ドキュメント、原著論文を優先し、`references/sources.json` に登録します。
- 出典を確認できない数値、製品仕様、現在の既定値、脅威動向を断定しません。
- 他書籍、講座、記事の文章やコードをコピーしません。引用は必要最小限にし、原則として自分の言葉で説明します。
- Notebookの出力を本文へ手作業で転記せず、結果オブジェクトやテストで再現できる値だけを使います。

## 9. 完了条件

変更は、次を満たして初めて完了です。

```bash
cd security-course
python -m pip install -e ".[dev]"
ruff check src tests examples scripts
ruff format --check src tests examples scripts
pytest
python scripts/validate_catalog.py
python scripts/validate_notebooks.py
python scripts/validate_references.py
python scripts/validate_traceability.py
```

加えて、PR本文に次を記載します。

- 対象章IDまたは基盤変更ID
- 変更した学習体験
- 仕様・コード・Notebook・原稿の変更範囲
- 安全境界の確認結果
- 実行した検証
- 既知の省略点と後続課題

## 10. 自律作業時の判断基準

情報が不足していても、安全で可逆な範囲は既存規約から合理的に補完し、作業を止めません。次のような不可逆または事業上の判断だけは、明示的な確認事項として残します。

- リポジトリ全体のライセンス変更またはデュアルライセンス化
- 別リポジトリへの分離
- 出版社、価格、商標、販売条件に関する決定
- 実環境を扱う教材への安全境界変更
- 公開APIの大規模な互換性破壊

それ以外は、小さなPR、テスト、仕様ID、明示的な仮定によって前進させます。
